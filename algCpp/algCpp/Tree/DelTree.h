#pragma once


//Delete the Tree using Post order traveral code. Means delete the node after using it.
/*


			void DeleteTree(NodeTree* rt)
			{
				NodeTree* cur = rt;
				if (cur)
				{
					DeleteTree(rt->R);


					DeleteTree(rt->L);
					if (rt)
					{
						delete rt;
					}
				}
			}

*/
